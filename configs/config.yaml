data:
  csv_path: data/music_sentiment_dataset.csv

features:
  drop_columns:
    - track_id

model:
  type: mlp
  hidden_dims: [128, 64]
  dropout: 0.2

training:
  batch_size: 64
  lr: 0.001
  epochs: 20
  device: cuda
  val_size: 0.2
  random_state: 42
  checkpoint_path: artifacts/best_mlp.pt
