data:
  csv_path: data/metadata.csv
  audio_dir: data/audio/
  sample_rate: 22050

features:
  n_mfcc: 40
  use_mel_spectrogram: true

model:
  type: cnn   # cnn | transformer | fusion
  num_classes: 4

training:
  batch_size: 32
  lr: 0.001
  epochs: 20
  device: cuda
  